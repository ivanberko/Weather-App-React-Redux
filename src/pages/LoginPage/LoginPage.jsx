import React from 'react';
import LoginForm from '../../components/LoginForm/LoginForm';

const LoginPage = () => (
  <section>
    <h2>Login Page</h2>
    <LoginForm />
  </section>
);

export default LoginPage;
